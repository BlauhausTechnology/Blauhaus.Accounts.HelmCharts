apiVersion: v1
kind: Secret 
metadata:
  name: {{ include "clientModuleName" . }}-secrets
type: Opaque
data:   
  {{ range .Values.customSecret }}
  {{ .name | quote }}: {{ .value | b64enc }}
  {{ end }}