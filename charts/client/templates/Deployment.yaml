apiVersion: apps/v1
kind: Deployment 
metadata:
  name: {{ include "clientModuleName" . }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: {{ include "clientModuleName" . }}-selector  
  minReadySeconds: 10   
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1     
  template:
    metadata:
      labels:
        app: {{ include "clientModuleName" . }}-selector
    spec:
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      imagePullSecrets:
      - name: secret-docker-auth
      containers:
      - name: {{ include "clientModuleName" . }}-container
        image: {{ include "customDockerImage" . }} 
        {{ include "resources" . }}
        volumeMounts:
          - name: {{ include "applicationEnvironment" . }}-openid-signing
            mountPath: "/{{ include "applicationEnvironment" . }}/signing"
            readOnly: true
          - name: {{ include "applicationEnvironment" . }}-openid-encryption
            mountPath: "/{{ include "applicationEnvironment" . }}/encryption"
            readOnly: true
        imagePullPolicy: Always
        ports: 
        - containerPort: {{ .Values.port }} 
        env:
        - name: ModuleName
          value: {{ include "clientModuleName" . }}
        {{ include "env_aspnetcore" .}}
        {{ include "env_logging" . }}
        {{ include "env_metadata" .}}
        {{ include "env_openidclient" .}}
        {{ include "env_custom" .}}
        {{ include "env_customsecret" .}}
        - name: ConnectionStrings__ApplicationInsights
          valueFrom:
            secretKeyRef:
              name: {{ include "applicationEnvironment" . }}-secrets
              key: ConnectionStrings--ApplicationInsights
        - name: ConnectionStrings__AzureStorage
          valueFrom:
            secretKeyRef:
              name: {{ include "applicationEnvironment" . }}-secrets
              key: ConnectionStrings--AzureStorage 
      volumes:
        - name: {{ include "applicationEnvironment" . }}-openid-signing
          secret:
            secretName: {{ include "applicationEnvironment" . }}-openid-signing
            optional: false
        - name: {{ include "applicationEnvironment" . }}-openid-encryption
          secret:
            secretName: {{ include "applicationEnvironment" . }}-openid-encryption
            optional: false
      
      